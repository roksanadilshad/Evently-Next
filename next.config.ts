/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i.pinimg.com", "lh3.googleusercontent.com"], // add any other domains here
  },
};

module.exports = nextConfig;
